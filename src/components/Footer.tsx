export default function Footer() {
  return (
    <footer className="site-footer">
      <p className="footer-text">
        <span className="prompt">$</span>
        AGENT ZERO PROTOCOL — YOUR FIRST AGENT ON-CHAIN — {new Date().getFullYear()}
      </p>
    </footer>
  )
}
